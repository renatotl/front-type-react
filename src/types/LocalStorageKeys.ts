export enum LocalStorageKeys {
    TOKEN = "token",
    USER = "user",
    ID = "id",
    PROFILE = "profile",
    GAME = "game",
  }

  //Vamos começar a integração com a API (backend) criando alguns arquivos necessários para essa integração.

  // o localStorage é como um banco de dados dentro do navegador
